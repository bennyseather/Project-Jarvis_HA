# Project Jarvis Search

This optional local service gives Jarvis a private SearXNG JSON search endpoint.
Start it before Project Jarvis and keep port 8088 available only on the trusted
home network. Jarvis retrieves only bounded public HTTP(S) pages and rejects
private, loopback, link-local, reserved, and multicast page targets.
