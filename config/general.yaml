project:
  name: "Project Jarvis"
  version: "0.1.0"

system:
  debug: false

logging:
  level: INFO

home_assistant:
  url: "http://homeassistant.local:8123"
  token: ""
  allowed_read_entities:
    - light.blocks
  action_policy:
    confirm_required:
      - light.turn_on
    high_impact: []
    allowed_entities:
      - light.blocks
